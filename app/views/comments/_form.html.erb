<%= form_with model: [ @articles, @articles.comments.build ] do |form| %>
  <p>
    <%= form.label :commenter %><br>
    <%= form.text_field :commenter%>
  </p>

  <p>
  <%= form.label :body%><br>
  <%= form.text_area :body, cols: 30, rows: 10%>
  </p>

  <p>
  <%= form.label :status %>
  <%= form.select :status, ['public', 'private', 'archived'], selected: 'public' %>
  </p>

  <p>
  <%= form.submit %>
  </p>
<% end %>